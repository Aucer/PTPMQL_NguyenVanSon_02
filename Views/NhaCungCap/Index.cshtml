@model IEnumerable<NguyenVanSon_02.Models.NhaCungCap>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MaNCC)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TenNCC)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MaNCC)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TenNCC)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaNCC">Sửa</a> |
                <a asp-action="Details" asp-route-id="@item.MaNCC">Chi tiết</a> |
                <a asp-action="Delete" asp-route-id="@item.MaNCC">Xoá</a>
            </td>
        </tr>
}
    </tbody>
</table>
